// Package scene holds the Backstage data model — Scene and Step (the JSON
// script) and Project (backstage.json: record/popup/env/hooks/aliases/layouts)
// — plus the find-up config loader and validation.
package scene
