// Package prompter types the floating instruction box on screen char-by-char
// (the auto-type "Prompter"). The Prompter interface abstracts the overlay
// backend; the current driver uses a Hyprland floating popup.
package prompter
