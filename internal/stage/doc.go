// Package stage builds and tears down the stage — a display plus the windows a
// scene runs in. The Stager interface lets the Hyprland driver (now) and a
// virtual-display driver (later) be swapped without touching the engine.
package stage
