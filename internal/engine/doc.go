// Package engine is the orchestrator: it runs a scene end to end (hooks, stage,
// record, steps, stop) over the stage/recorder/prompter/pane drivers, shared by
// the play and rehearse verbs.
package engine
